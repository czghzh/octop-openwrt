from setuptools import setup, find_packages

setup(
    name="playwright",
    version="1.99.0",
    description="Minimal stub for playwright (Octop musl/aarch64 workaround)",
    packages=find_packages(),
)
